<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="initial-scale=1, width=device-width" />
    <link rel="stylesheet" href="css/global.css" />
    <link rel="stylesheet" href="css/new_password.css" />
    <link rel="stylesheet" href="css/all_buttons.css" />
    <title><%= pageTitle %></title>
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Inter:wght@500;600;700&display=swap"
    />
    <style>
      body {
        background-color: var(--primary-color);
      }
    </style>
  </head>
  <body>
    <div class="main-login">
      <form class="frame-parent" id="frameForm" action="/reset" method="POST">
        <p>Enter your Email Address to get a reset password link.</p>
        <input
          class="frame-item"
          type="email"
          name="email"
          required
          placeholder="Enter your Email"
          
        />
        <button class="login-wrapper login disabled-button" type="submit">
          Send Mail
        </button>
        <% if (errorMessage) { %>
          <div class="user-message error-stmt"><%= errorMessage %></div>
          <% } %>
      </form>
    </div>
  </body>
</html>



