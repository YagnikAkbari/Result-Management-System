<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="initial-scale=1, width=device-width" />
    <link rel="stylesheet" href="/css/global.css" />
    <link rel="stylesheet" href="/css/login.css" />
    <link rel="stylesheet" href="/css/new_password.css" />
    <link rel="stylesheet" href="/css/all_buttons.css" />
    <title><%= pageTitle %></title>
    <style>
      body {
        background-color: var(--primary-color);
      }
    </style>
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Inter:wght@500;600;700&display=swap"
    />
  </head>
  <body>
    <div class="main-login">
      <form
        class="frame-parent"
        id="frameForm"
        action="/reset-first-time"
        method="POST"
      >
        <p>
          You are logging in for the first time, kindly update your password.
        </p>
        <input
          class="frame-item"
          type="password"
          name="pass"
          placeholder="Enter new password"
          id="password"
        />
        <input
          class="frame-item"
          type="password"
          name="confirmPass"
          placeholder="Confirm new password"
          id="password"
        />
        <button class="login-wrapper login disabled-button" type="submit">
          Update Password
        </button>
        <% if (errorMessage) { %>
        <div class="user-message error-stmt"><%= errorMessage %></div>
        <% } %>
      </form>
    </div>
  </body>
</html>
